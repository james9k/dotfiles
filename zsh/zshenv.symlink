typeset -U path
path=(${HOME}/bitbucket/myautomation/bin ${HOME}/bin ${HOME}/.dotfiles/bin ${HOME}/.cargo/bin ${HOME}/.local/bin $path[@])

if [ "$(uname)" = "Darwin" ] ; then
    path=(${HOME}/Library/Python/3.7/bin $path[@])
fi
